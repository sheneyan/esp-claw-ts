diff --git a/src/ml_stun.c b/src/ml_stun.c
--- a/src/ml_stun.c
+++ b/src/ml_stun.c
@@ -127,0 +128 @@ static esp_err_t ensure_stun_socket(microlink_t *ml) {
+    ml_bind_sock_to_upstream(ml, ml->stun_sock);
@@ -474,0 +476 @@ static esp_err_t ensure_stun_socket6(microlink_t *ml) {
+    ml_bind_sock_to_upstream(ml, ml->stun_sock6);
diff --git a/src/ml_coord.c b/src/ml_coord.c
--- a/src/ml_coord.c
+++ b/src/ml_coord.c
@@ -86,0 +87,13 @@ void ml_bind_sock_to_upstream(microlink_t *ml, int fd)
+static struct ifreq *ml_upstream_ifreq(microlink_t *ml, struct ifreq *ifr)
+{
+    if (!ml || !ifr || !ml->upstream_netif) return NULL;
+    struct netif *up = (struct netif *)ml->upstream_netif;
+    if (!netif_is_up(up) || !netif_is_link_up(up)) return NULL;
+
+    memset(ifr, 0, sizeof(*ifr));
+    netif_index_to_name(netif_get_index(up), ifr->ifr_name);
+    if (ifr->ifr_name[0] == '\0') return NULL;
+
+    return ifr;
+}
+
@@ -329,0 +343 @@ static int fetch_server_pubkey(microlink_t *ml, const char *host, const char *port) {
+        struct ifreq tls_if;
@@ -333,0 +348 @@ static int fetch_server_pubkey(microlink_t *ml, const char *host, const char *port) {
+            .if_name           = ml_upstream_ifreq(ml, &tls_if),
@@ -758,0 +774 @@ static int do_tcp_connect(microlink_t *ml) {
+        struct ifreq tls_if;
@@ -762,0 +779 @@ static int do_tcp_connect(microlink_t *ml) {
+            .if_name           = ml_upstream_ifreq(ml, &tls_if),
